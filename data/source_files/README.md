# Source-file intake

This clean build contains no company data. Drag CSV, XLSX, XLSM or PDF files anywhere onto the Data management visual. LedgerFlow uploads them, creates source nodes, extracts governed products and routes them to department agents.

You can also paste files into `permanent/` or `recurring/` while LedgerFlow is stopped; the folder scanner will queue them after startup.
