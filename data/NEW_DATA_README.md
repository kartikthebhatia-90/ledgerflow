# Empty LedgerFlow data directory

This package contains no company records, source documents, generated contexts, agent history or runtime databases. Paste your `.env`, start the app and drag files onto the Data management visual. Runtime databases and derived context are created locally under this directory.
