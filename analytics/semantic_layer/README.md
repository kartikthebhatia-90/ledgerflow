# LedgerFlow analytics semantic layer

This directory is the inspectable contract for LedgerFlow v1.0. It defines canonical metrics, source precedence, dashboard roles, setup gates and executable agent actions.

The application treats this layer as a map, not a substitute for source validation. Metrics remain blocked or provisional when their required evidence is missing or incomplete.
